class Solution {
public:
    int strStr(string haystack, string needle) {
    if(needle.length()==0)
        return 0;
        int found = haystack.find(needle); 
	if (found != haystack.length()) 
        return found;
        else
            return -1;
    }
};
